# Copy this file to .env and edit with your values.
# To view your env: open .env in your editor, or run: cat .env (from project root)

# Server port (optional, default 3000)
PORT=3000

# SQLite DB file path (optional).
# Useful on hosts like Render if you attach a persistent disk mounted at /var/data.
# Example: SQLITE_DB_PATH=/var/data/store.db
SQLITE_DB_PATH=

# Admin session secret (recommended in production so logins keep working across restarts/scaling).
# Example: SESSION_SECRET=change-me-to-a-long-random-string
SESSION_SECRET=

# Paddle client-side token (required for Paddle.js checkout).
# Create one in Paddle dashboard → Developer tools → Authentication.
# Token starts with test_ (sandbox) or live_ (production).
PADDLE_CLIENT_TOKEN=
